[package]
name = "hotwire-runner"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Safe command execution: review, sanitized env, timeouts, cancellation, redacted logs"

[dependencies]
home = "0.5"
nix = { version = "0.30", features = ["process", "signal"] }
serde.workspace = true
thiserror.workspace = true
tokio = { workspace = true, features = ["macros", "process", "rt", "io-util", "sync", "time"] }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
